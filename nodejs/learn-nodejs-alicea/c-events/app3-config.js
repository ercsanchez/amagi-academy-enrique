// coding pattern: create a config file that contains all of the events (property names) 
// making event property names more robust and preventing typo errors

module.exports = {
    events: {
        GREET: 'greet',
        FILESAVE: 'fileSaved',
    }
}